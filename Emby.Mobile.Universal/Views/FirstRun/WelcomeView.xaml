<controls:EmbyPage x:Class="Emby.Mobile.Universal.Views.FirstRun.WelcomeView"
                   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                   xmlns:local="using:Emby.Mobile.Universal.Views.FirstRun"
                   xmlns:controls="using:Emby.Mobile.Universal.Controls"
                   xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                   xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                   mc:Ignorable="d">

    <Grid>
        <Grid.Background>
            <ImageBrush Stretch="UniformToFill"
                        ImageSource="ms-appx:///Assets/Backdrops/Splash_Screen_Landscape_1920x1080_nologo.png" />
        </Grid.Background>
        <RelativePanel>
            <Grid x:Name="Logo"
                  RelativePanel.AlignLeftWithPanel="True"
                  RelativePanel.AlignRightWithPanel="True"
                  RelativePanel.AlignTopWithPanel="True">
                <Border Background="{ThemeResource SmokeContrastBrush}"
                        Opacity="0.7" />
                <controls:EmbyImage Margin="10,30"
                                    MaxHeight="60"
                                    Stretch="UniformToFill"
                                    Tag="{ThemeResource EmbyLogo}"
                                    Source="{Binding Tag, RelativeSource={RelativeSource Self}}" />
            </Grid>
            <!-- #region Welcome (Step 0)  -->
            <TextBlock x:Name="WelcomeLabel"
                       RelativePanel.Below="Logo"
                       RelativePanel.AlignLeftWithPanel="True"
                       RelativePanel.AlignRightWithPanel="True"
                       Text="{x:Bind Converter={StaticResource LocalizationConverter}, ConverterParameter=LabelWelcomeText}"
                       TextWrapping="WrapWholeWords"
                       Margin="10,50,10,100"
                       Opacity="0"
                       Style="{StaticResource NormalTextBlockStyle}" />
            <!--#endregion-->
            <!-- #region Server info (Step 1) -->
            <TextBlock x:Name="ServerInfoLabel"
                       Text="{x:Bind Converter={StaticResource LocalizationConverter}, ConverterParameter=LabelFirstRunServerInfo_v2}"
                       RelativePanel.Below="Logo"
                       RelativePanel.AlignLeftWithPanel="True"
                       RelativePanel.AlignRightWithPanel="True"
                       TextWrapping="WrapWholeWords"
                       Margin="10,20,10,10"
                       Style="{StaticResource NormalTextBlockStyle}"
                       Opacity="0" />
            <HyperlinkButton x:Name="VisitEmbySiteButton"
                             Content="{x:Bind Converter={StaticResource LocalizationConverter}, ConverterParameter=LabelVisitEmbyWebsite}"
                             RelativePanel.Below="ServerInfoLabel"
                             HorizontalAlignment="Center"
                             Margin="0,10,0,20"
                             RelativePanel.AlignHorizontalCenterWithPanel="True"
                             ClickMode="Release"
                             Click="VisitEmbySiteButton_Click"
                             Opacity="0" />
            <!--#endregion-->
            <!-- #region About Connect (Step 3) -->
            <TextBlock x:Name="AboutConnect"
                       Text="{x:Bind Converter={StaticResource LocalizationConverter}, ConverterParameter=LabelMbConnectDescriptionPara1}"
                       RelativePanel.Below="Logo"
                       RelativePanel.AlignLeftWithPanel="True"
                       RelativePanel.AlignRightWithPanel="True"
                       TextWrapping="WrapWholeWords"
                       Opacity="0"
                       Visibility="Collapsed"
                       Margin="10,20,10,0"
                       Style="{StaticResource NormalTextBlockStyle}" />
            <TextBlock x:Name="HasAccountText"
                       Text="{x:Bind Converter={StaticResource LocalizationConverter}, ConverterParameter=LabelEmbyConnectAlreadyGotAccount}"
                       RelativePanel.Below="AboutConnect"
                       Opacity="0"
                       Visibility="Collapsed"
                       RelativePanel.AlignLeftWithPanel="True"
                       RelativePanel.AlignRightWithPanel="True"
                       TextWrapping="WrapWholeWords"
                       Margin="10"
                       Style="{StaticResource SmallTextBlockStyle}" />
            <Button x:Name="LoginButton"
                    Style="{StaticResource EmptyButtonStyle}"
                    RelativePanel.AlignHorizontalCenterWithPanel="True"
                    RelativePanel.Below="HasAccountText"
                    ClickMode="Release"
                    Opacity="0"
                    Visibility="Collapsed"
                    Click="LoginButton_Click">
                <StackPanel Orientation="Horizontal">
                    <Border CornerRadius="30"
                            Height="60"
                            Width="60"
                            Background="{StaticResource EmbyGreenBrush}"
                            Margin="10,0">
                        <TextBlock Style="{StaticResource Mdl2Style}"
                                   Text="{StaticResource Account}"
                                   FontSize="30"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Foreground="White" />
                    </Border>
                    <TextBlock Text="{x:Bind Converter={StaticResource LocalizationConverter}, ConverterParameter=LabelLogIn}"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Style="{StaticResource LargeThinTextBlockStyle}" />
                </StackPanel>
            </Button>
            <TextBlock x:Name="SignUpText"
                       Text="{x:Bind Converter={StaticResource LocalizationConverter}, ConverterParameter=LabelMbConnectFreeAccount}"
                       RelativePanel.Below="LoginButton"
                       RelativePanel.AlignLeftWithPanel="True"
                       RelativePanel.AlignRightWithPanel="True"
                       Opacity="0"
                       Visibility="Collapsed"
                       TextWrapping="WrapWholeWords"
                       Margin="10,10,10,0"
                       Style="{StaticResource SmallTextBlockStyle}" />
            <HyperlinkButton x:Name="SignUpButton"
                             Content="{x:Bind Converter={StaticResource LocalizationConverter}, ConverterParameter=LabelSignUp}"
                             RelativePanel.Below="SignUpText"
                             HorizontalAlignment="Center"
                             Margin="0,10,0,20"
                             Opacity="0"
                             Visibility="Collapsed"
                             RelativePanel.AlignHorizontalCenterWithPanel="True"
                             ClickMode="Release"
                             Click="SignUpButton_Click" />
            <!--#endregion-->

            <controls:EmbyImage RelativePanel.AlignBottomWithPanel="True"
                                RelativePanel.AlignLeftWithPanel="True"
                                RelativePanel.AlignRightWithPanel="True"
                                Opacity="0.3"
                                Height="120"
                                Stretch="UniformToFill"
                                Source="ms-appx:///Assets/Backdrops/EmbyRandomAvatars.jpg" />
            <Button x:Name="NextButton"
                    Style="{StaticResource EmptyButtonStyle}"
                    RelativePanel.AlignBottomWithPanel="True"
                    RelativePanel.AlignHorizontalCenterWithPanel="True"
                    Margin="20"
                    ClickMode="Release"
                    Click="NextButton_Click">
                <StackPanel Orientation="Horizontal">
                    <Border CornerRadius="30"
                            Height="60"
                            Width="60"
                            Margin="10"
                            Background="{StaticResource EmbyGreenBrush}">
                        <TextBlock Style="{StaticResource Mdl2Style}"
                                   Text="{StaticResource ArrowRight}"
                                   FontSize="30"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Foreground="White" />
                    </Border>
                    <TextBlock Text="{x:Bind Converter={StaticResource LocalizationConverter}, ConverterParameter=AppBarNext}"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Style="{StaticResource LargeThinTextBlockStyle}" />
                </StackPanel>
            </Button>
            <Button x:Name="ManualConnectButton"
                    Style="{StaticResource EmptyButtonStyle}"
                    RelativePanel.AlignBottomWithPanel="True"
                    RelativePanel.AlignHorizontalCenterWithPanel="True"
                    Margin="20,20,20,40"
                    ClickMode="Release"
                    Opacity="0"
                    Visibility="Collapsed"
                    Click="ManualConnectButton_Click">
                <StackPanel Orientation="Horizontal">
                    <Border CornerRadius="20"
                            Height="40"
                            Width="40"
                            Background="{ThemeResource EmbyGrayButtonBrush}"
                            Margin="10,0">
                        <TextBlock Style="{StaticResource Mdl2Style}"
                                   Text="{StaticResource Pen}"
                                   FontSize="20"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Foreground="White" />
                    </Border>
                    <TextBlock Text="{x:Bind Converter={StaticResource LocalizationConverter}, ConverterParameter=LabelSkip}"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Style="{StaticResource LargeThinTextBlockStyle}" />
                </StackPanel>
            </Button>
        </RelativePanel>
    </Grid>
</controls:EmbyPage>
