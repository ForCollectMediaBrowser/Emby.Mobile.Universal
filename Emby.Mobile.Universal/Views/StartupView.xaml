<controls:EmbyPage x:Class="Emby.Mobile.Universal.Views.StartupView"
                   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                   xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                   xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                   xmlns:controls="using:Emby.Mobile.Universal.Controls"
                   xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
                   xmlns:core="using:Microsoft.Xaml.Interactions.Core"
                   mc:Ignorable="d"
                   d:DataContext="{Binding Startup, Source={StaticResource Locator}}">
    <interactivity:Interaction.Behaviors>
        <core:EventTriggerBehavior EventName="Loaded">
            <core:InvokeCommandAction Command="{Binding PageLoadedCommand}" />
        </core:EventTriggerBehavior>
    </interactivity:Interaction.Behaviors>

    <Grid Background="{ThemeResource BackgroundBrush}">
        <RelativePanel>
            <Image x:Name="SplashScreenImage"
                   Source="/Assets/SplashScreen.png"
                   Height="300"
                   RelativePanel.AlignHorizontalCenterWithPanel="True"
                   RelativePanel.AlignVerticalCenterWithPanel="True" />
            <ProgressRing x:Name="ProgressRing"
                          Visibility="Visible"
                          RelativePanel.Below="SplashScreenImage"
                          RelativePanel.AlignHorizontalCenterWithPanel="True"
                          IsActive="{Binding ProgressIsVisible}"/>
            <TextBlock Text="{Binding ProgressText}"
                       RelativePanel.Below="ProgressRing"
                       RelativePanel.AlignHorizontalCenterWithPanel="True" />
        </RelativePanel>
    </Grid>
</controls:EmbyPage>
